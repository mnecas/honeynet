
ssh mnecas@192.168.0.99 -i keys/id_ed25519 -p 35695 -L 8000:localhost:8000

scp -i honeypot-data-server/keys/id_ed25519 -P 35695 -r honeypot-data-server/
mnecas@192.168.0.99:/home/mnecas/

firewall-cmd --zone=public --add-service=http

semanage permissive -a haproxy_t

python3 manage.py runserver

tshark -r capture.pcap -T ek > packets.json

# apt install openvswitch-common openvswitch-switch -y
# ovs-vsctl add-br mybridge
# ip link set mybridge up

# ovs-vsctl add-port mybridge enp1s0
# ip addr flush dev enp1s0
# dhclient mybridge

# Adding interface to docker
# ip tuntap add mode tap vport1
# ip link set dev vport1 up
# ovs-docker add-port mybridge vport1 myContainer1
#

# ip a
# apt install iproute2 -y
# --cap-add=NET_ADMIN
# ping
# apt install iputils-ping -y


# ip link add <p1-name> netns <p1-ns> type veth peer <p2-name> netns <p2-ns>

# iptables -A FORWARD -i wlan0 -o wlan1

# ip link set eth1 promisc on

# network_id=$(docker network inspect -f {{.Id}} <network name>)
# bridge_name="br-${network_id:0:12}"
